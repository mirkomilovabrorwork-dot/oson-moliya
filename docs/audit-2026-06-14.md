# Audit + handoff — 2026-06-14 (session ended on Claude usage limit)

Durable record so NOTHING is lost. Resume from `docs/STATE.md` ⚡ STATUS, then this file.

## What is ACTUALLY done vs not (honest)
- ✅ **DONE for real:** ElevenLabs Scribe v2 STT switched + deployed LIVE to prod
  (oson-moliya.vercel.app, dpl_G3pVmFmNy72qyDQyWK6SnE4ziAj9, READY). Prod env STT_PROVIDER=elevenlabs
  + ELEVENLABS_API_KEY fixed via Vercel REST API upsert (were empty). Brain stays Claude (sealed).
- ⚠️ **FOUND but NOT FIXED:** bot audit gaps below — nothing was patched yet.
- ❌ **NOT STARTED:** dashboard + docs audit (was interrupted before running). Ready-to-run prompt below.
- ❓ **NOT CONFIRMED:** that voice actually transcribes correctly now — needs a real Uzbek voice test by the user.

---

## Bot audit — DONE (read-only). 10/11 capabilities fully wired.
| # | Capability | Status | Evidence (src/lib/telegram/bot.ts unless noted) |
|---|---|---|---|
| 1 | Text log → record_intent → save → localized confirm | WIRED | bot.ts:247–303; brain.ts:46–53; finalizeLog:79–175; reply.ts:132–175 |
| 2 | Voice: OGG download, configured STT, "voice.ogg", language passed | WIRED | bot.ts:667–735, 716–718; stt/index.ts:18–27; groq.ts:30–31 |
| 3 | Smart categorization ("5000 som lavash"→Oziq-ovqat; buttons if unsure) | WIRED | prompts.ts:59–82; bot.ts:337–384; brain.ts:100–113 |
| 4 | Inline buttons: type-clarify, category, delete-confirm | WIRED | bot.ts:738–917 (ownership-checked, answerCallbackQuery, try/catch) |
| 5 | Correction + deletion via text | WIRED | bot.ts:419–487 (correct), 490–531 (soft-delete) |
| 6 | Finance queries → safe aggregation | WIRED | bot.ts:393–416; analytics.ts:180–199 (validated enums, no SQLi) |
| 7 | Foreign-currency guard ("100 dollar"→clarify) | WIRED | brain.ts:89–113; prompts.ts:26–31 |
| 8 | Budget alerts on expense write, once/month | WIRED | bot.ts:141–159; budgets.ts:40–101 |
| 9 | Language detect/reply uz/ru/en | WIRED | brain.ts:27–53; bot.ts:237–244; reply.ts:22–82 |
| 10 | Debts/Accounts via BOT | **MISSING** | services exist (debts.ts, accounts.ts) but no bot intents/commands — dashboard-only (intentional?) |
| 11 | Webhook secret-token check, always 200, idempotency | WIRED (idempotency not guarded) | api/telegram/route.ts:9–34 |

### Ranked bot gaps to decide on (none block submission; confirm with user)
1. **Debts/Accounts dashboard-only** — no bot integration. Confirm OK to leave.
2. **Category buttons capped at 6** (bot.ts ~360 `take: 6`) — users with >6 categories can't reach the rest via
   buttons (can still type or "✏️ Boshqa"). Low-effort fix if a grader tests it.
3. **Pending-draft reset edge case** — after "✏️ Boshqa", if the brain re-reads the typed text as ambiguous
   (category=null), the draft category is wiped → re-clarify loop. (bot.ts:318–334)
4. **Webhook idempotency not guarded** — Telegram retry would double-log. Rare; needs a migration. (deferred)
5. **message:audio defaults to .mp3** (bot.ts ~973) — OGG sent as audio (not voice) may degrade STT.
6. **Correction `target` only handles "last"** — "second-to-last" unsupported (schema has the field, handler ignores).
7. **Single-word messages default to uz** — e.g. "да" replies in Uzbek.

---

## NEXT: dashboard + docs audit — NOT RUN YET. Re-run this Explore-agent prompt verbatim:
> Audit the Next.js dashboard, API routes, and submission docs of PulTrack for ANY incomplete/placeholder/broken
> piece. Repo root: C:\Users\localhost\Desktop\pultrack. Verify with file:line:
> 1. Every page under src/app/(dashboard)/* + src/app/* renders REAL data, not placeholder. Grep repo for
>    "Tez orada","coming soon","TODO","FIXME","placeholder","Lorem". Pages: Home(/), transactions(Yozuvlar),
>    analytics, categories, debts, accounts, onboarding, more(Yana), login.
> 2. Every src/app/api/* route: getSessionUser auth, owner-scoped (filters by userId), zod/422 validation, and
>    assertSameOrigin on mutating routes. Flag any missing control (PUBLIC repo = security matters).
> 3. BigInt serialization on every money response.
> 4. i18n uz/ru/en: no missing keys for debts/accounts/Yana; no hardcoded user-facing strings.
> 5. Submission artifacts: README (links resolve, env/webhook/architecture), docs/demo-script.md (matches prod),
>    .env.example (incl. ELEVENLABS_API_KEY, STT_PROVIDER), product brief + "3 more days" paragraph.
> 6. Dark-theme hydration fix present (no flash). 7. TelegramBackButton mounted in layout.
> Do NOT edit. Return ranked gaps with file:line.

Then fix any real gap, run gates (`npm run typecheck && npm test && npx next build`), commit, redeploy.

## USER-only actions (gate submission confidence)
1. **Test a real Uzbek voice msg** to @oson_moliya_bot → confirm ElevenLabs transcription.
2. **Spend caps** on Anthropic + Groq + ElevenLabs keys (bot is PUBLIC).
3. **Record demo video** per docs/demo-script.md.
4. **Rotate the GitHub PAT** (it appeared in terminal output; it's only in local .git/config, not the public repo, so currently safe).

## Commit hygiene
- Uncommitted at handoff: docs/tasks/017 (modified), docs/design-experiment/ (untracked — leave UNTRACKED per policy).
